- use postgres
------
* syntax examples
  #+begin_src scala
  Cell("asd") <= Content()

  val propagator = Propagator()

  propagator
    .from(Cell :: Cell :: HNil)
    .to(Cell)

  val cell = propagator(Cell, Cell)
  #+end_src
